apiVersion: v2
name: api-customers
description: api-customers umbrella chart (devci) — pins the versioned dotnet-api common chart
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  # Bump per environment to roll out common-chart changes independently (devci first).
  - name: dotnet-api
    version: 1.0.0
    repository: oci://registry-1.docker.io/fchanteau95/dotnet-api
