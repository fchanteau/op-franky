apiVersion: v2
name: api-customers
description: api-customers umbrella chart (prod) — pins the versioned dotnet-api common chart
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  # Keep prod on a proven version; bump only after devci/uatrc validation.
  - name: dotnet-api
    version: 1.0.0
    repository: oci://registry-1.docker.io/fchanteau95
